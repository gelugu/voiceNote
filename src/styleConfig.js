import { Dimensions } from "react-native";

export const deviceWidth = Dimensions.get("screen").width;
export const deviceHeight = Dimensions.get("screen").height;

export const deviceStatusBarHeight = 56;

export const colors = {
  background: "#e8eddf",
  primary: "#f5cb5c",
  secondary: "#cfdbd5",
  text: "#242423",
  dark: "#333533",
};
